# 服务器端恶意检测（余弦相似度）设计文档

## 1. 背景与目标
在联邦学习中，客户端会主动发起“标签修改请求”（把类别 A 改成 B）。  
服务器必须先判断该请求是否合法，防止恶意客户端伪造修改、植入后门或投毒。  
本文档描述的 **余弦相似度检测** 机制，仅需客户端上传“类条件梯度方向”和“类条件平均损失”两个极小量，即可在服务器端给出每个类别的连续污染分数 `p_c ∈ [0,1]`，并输出可疑客户端集合。

---

## 2. 客户端每轮上传内容（极轻量）

| 字段 | 含义 | 大小估算 |
|------|------|----------|
| `ĝ_{k,c}` | 类 `c` 的平均梯度 **单位向量**（方向） | `d` 维 float32，压缩后 ≈ 0.5 MB / 100 类 |
| `ℓ_{k,c}` | 类 `c` 的平均损失标量 | 1 个 float32 |

*不上传原始数据、标签、完整梯度，满足隐私与带宽要求。*

---

## 3. 服务器端检测流程（大白话版）

1. **收集同一类的所有方向向量**  
   把所有参与本轮的客户端对类 `c` 上传的单位向量 `ĝ` 画在“高维单位球面”上。正常客户端会抱团成一个紧凑的簇。

2. **计算历史平均方向**  
   维护滑动窗口（最近 W 轮），对每个类 `c` 计算历史平均方向向量 `μ_c`（单位向量）。

3. **计算余弦相似度**  
   对当前轮每个客户端的方向 `ĝ_{k,c}`，与历史平均方向 `μ_c` 算余弦相似度：
   ```
   cos_sim = ĝ_{k,c} · μ_c  ∈ [-1, 1]
   ```
   - `cos_sim ≈ 1`：方向一致，正常
   - `cos_sim < cos_th`：方向偏离，可疑

4. **给基础分 `base_c`**
   
   | 情况 | 判断逻辑 | `base_c` 计算 |
   |------|----------|---------------|
   | 所有客户端 `cos_sim ≥ cos_th` | 完全正常 | `0` |
   | 部分客户端 `cos_sim < cos_th` | 存在方向分裂 → 疑似恶意 | `1 - 平均有效cos_sim` |
   | 全部 `cos_sim` 均较低 | 整体漂移（Non-IID 正常演化） | `0`（不惩罚整体漂移） |

5. **损失侧兜底 `loss_bonus_c`**  
   若当前类平均损失比历史均值高出 `τ_loss` 个标准差 → 给固定 **0.5** 底分。

6. **融合得到最终污染分数**  
   `p_c = min(base_c + loss_bonus_c , 1.0)`

7. **产出可疑客户端集合 `S_c`**  
   方向与历史均值夹角超过 `τ_angle`（即 `cos_sim < cos(τ_angle)`）的客户端标记为可疑。

---

## 4. 关键超参与建议取值

| 符号 | 含义 | 建议范围 | 调节原则 |
|------|------|----------|----------|
| `cos_th` | 余弦相似度阈值 | 0.85 ~ 0.95 | 误报多 → 调高（如 0.95）；漏报多 → 调低（如 0.8） |
| `W` | 历史滑动窗口轮数 | 5 ~ 10 | 轮次少 → 减小 |
| `τ_hist` | 整体漂移容忍（兼容旧参数） | 20° ~ 30° | 误报多 → 调大 |
| `τ_var` | 方向方差上限（兼容旧参数） | 0.02 ~ 0.05 | 同上 |
| `τ_loss` | 损失跳变倍数（×σ） | 1.5 ~ 2.0 | 验证集损失分布定 |
| `τ_angle` | 可疑客户端夹角阈值 | 30° ~ 45° | 误报/漏报权衡 |

---

## 5. 污染分数 `p_c` 的下游用法（直接对接遗忘流水线）

| 下游模块 | 如何用 `p_c` |
|----------|--------------|
| **类级参数掩码 `M_c`** | `M_c = Top‑k(|g_c|, ratio = p_c)` —— 越脏，掩盖比例越大 |
| **带界微调轮数 `T_c`** | `T_c = ceil(T_max · p_c)` —— 干净类 0 轮，最脏类 `T_max` 轮 |
| **理论界 1：Rényi 遗忘界 `ε_c`** | `ε_c = ε₀ · p_c` |
| **理论界 2：Anti‑Relearning 正则 `λ_mem,c`** | `λ_mem,c = λ₀ · p_c` |

---

## 6. 算法伪代码（可直接放入论文）

```text
Algorithm 1 Legitimacy Verification via Cosine Similarity
Input: {ĝ_{k,c}, ℓ_{k,c}}_{k=1}^K,
       historical μ^{hist}_c, L^{hist}_c, σ^{hist}_c,
       hyper-params cos_th, W, τ_loss, τ_angle
Output: pollution scores {p_c}, suspect sets {S_c}

for c = 1 … C do
    // 1. 历史平均方向
    μ_c ← mean_{w∈window} (mean_{k} ĝ_{k,c}^{(w)})
    μ_c ← μ_c / ‖μ_c‖
    
    // 2. 当前轮余弦相似度
    cos_sims ← { ĝ_{k,c} · μ_c | k = 1..K }
    
    // 3. 基础分
    valid_sims ← [s for s in cos_sims if s ≥ cos_th]
    if len(valid_sims) == K then
        base_c ← 0
    else
        base_c ← 1 - mean(valid_sims) if valid_sims else 1
    end if
    
    // 4. 损失兜底
    L_c ← mean_k ℓ_{k,c}
    loss_bonus_c ← 0.5 if L_c > L^{hist}_c + τ_loss·σ^{hist}_c else 0
    
    // 5. 最终分
    p_c ← min(base_c + loss_bonus_c, 1)
    
    // 6. 可疑集合
    S_c ← { k | ĝ_{k,c} · μ_c < cos(τ_angle) }
end for
return {p_c, S_c}_{c=1}^C
```

---

## 7. 给评审/开题的一句话判定逻辑

> “对每个类别，用历史滑动窗口维护类条件梯度的**平均方向向量**；当前轮客户端上传的方向与该历史均值算**余弦相似度**：  
> 全部接近 1 → 污染分 0；  
> 部分显著偏离（`cos_sim < cos_th`） → 按偏离比例给分；  
> 损失异常跳变 → 叠加底分。  
> 最终得到连续分数 `p_c ∈ [0,1]`，直接驱动类级掩码宽度、微调轮数、以及两条理论界的参数，实现『检测—遗忘—理论』端到端闭环。”

---

## 8. 后续可做的消融实验（建议）

1. **去掉历史均值**（仅用当前轮均值）→ `p_c` 退化，遗忘效果下降。  
2. **去掉 `loss_bonus`** → 隐蔽标签翻转漏报上升。  
3. **扫描 `cos_th / τ_angle / τ_loss`** → 画 Precision‑Recall 曲线，选最佳工作点。  
4. **极端 Non‑IID (α=0.01)** 小规模实验 → 观察误报率，必要时增大 `W` 或 `cos_th`。  

---

*文档生成时间：2026‑07‑20*  
*存放路径：`/mnt/d/VS Code 项目/label_modify_fl/服务器端恶意检测.md`*  
*可直接在 VS Code 中打开阅读、编辑、转换为 PDF/Word。*