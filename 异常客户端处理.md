# 异常客户端处理（余弦相似度检测后）设计文档

## 1. 背景
服务器通过 **余弦相似度检测**（见《服务器端恶意检测.md》）得到：
- 每类污染分数 `p_c ∈ [0,1]`
- 可疑客户端集合 `S_c`

本文档描述：对可疑客户端执行 **类级外科手术式遗忘**，对良性客户端执行 **带界聚合**。

---

## 2. 类级掩码构建（核心创新点）

对每个可疑类 `c ∈ S_c`：
```
M_c = Top‑k( |ĝ_c| , ratio = p_c )
```
- `p_c` 越大（越脏） → 掩盖比例越大
- `p_c = 0`（干净类） → `M_c = ∅`，不掩盖任何参数
- 掩码下发给所有客户端，**锁死 `M_c` 内参数**，仅更新 `M_c` 外参数

---

## 3. 可疑客户端隔离 + 外科手术式遗忘

### 3.1 隔离
- 可疑客户端集合 `S = ∪_c S_c`
- 这些客户端**本轮不参与聚合**，仅在本地执行遗忘

### 3.2 遗忘目标
对可疑类 `c ∈ S_c`，让全局模型 **“只忘掉类 c 的知识，保留其余类”**。

### 3.3 遗忘算法（梯度反转 + 掩码约束）
```python
def surgical_unlearn(model, clean_loader, mask_M, unlearn_epochs=5, lr=0.01):
    optimizer = SGD(params_not_in_M, lr=lr)  # 仅优化非掩码参数
    for epoch in range(unlearn_epochs):
        for x, y in clean_loader:
            logits = model(x)
            # 仅计算可疑类的损失，并取反
            loss = -CE(logits[y==c], y[y==c])
            loss.backward()
            # 掩码内参数梯度置零
            for p in masked_params:
                p.grad.zero_()
            optimizer.step()
```
**关键点**：
- 仅优化 **非掩码参数** → 保护干净类知识
- 损失取反 → 推离可疑类决策边界
- 掩码内梯度置零 → 绝不触碰被锁参数

---

## 4. 良性客户端带界聚合

对未被标记的良性客户端：
```
w_k = 1 / (1 + p_{max})        # p_{max} = max_c p_c，越脏权重越低
w_k = w_k / sum(w_k)
global = TrimmedMean({w_k · Δ_k}, trim_ratio = 0.2)
```
双重保险：
1. 加权平均按污染度加权
2. Trimmed‑Mean 再剔除极端更新

---

## 5. 遗忘后的理论界自动同步

| 理论界 | 参数自动化映射 |
|--------|----------------|
| Rényi 遗忘界 `ε_c` | `ε_c = ε₀ · p_c` |
| Anti‑Relearning 正则 `λ_mem,c` | `λ_mem,c = λ₀ · p_c` |
| 掩码比例 `ratio_c` | `ratio_c = p_c · ratio_max` |

所有界参数 **零额外超参**，完全由 `p_c` 驱动。

---

## 5. 算法伪代码（完整流程）

```text
Input: {ĝ_{k,c}, ℓ_{k,c}}, model θ
Output: updated θ

# 1. 检测
{p_c, S_c} ← CosineSimilarityDetect({ĝ, ℓ})

# 2. 构建掩码
for c in classes:
    if p_c > 0:
        M_c ← TopK(|ĝ_c|, p_c · mask_ratio_max)
    else:
        M_c ← ∅

# 3. 隔离可疑客户端
S ← ∪_c S_c
benign ← all_clients \ S

# 4. 良性客户端聚合
w ← weight(benign, p_c)
θ_agg ← TrimmedMean(benign_updates, w)

# 5. 可疑客户端本地遗忘
for c in classes where p_c > 0:
    θ_agg ← SurgicalUnlearn(θ_agg, clean_loader, M_c,
                            epochs=p_c·max_epochs,
                            lr=p_c·max_lr)

return θ_agg
```

---

## 6. 实验验证点（给评审用）

| 验证目标 | 实验设计 | 成功标准 |
|----------|----------|----------|
| 掩码精准度 | 对比 `M_c` 覆盖的参数 vs. 真实受污染参数 | F1 ≥ 0.9 |
| 遗忘干净度 | 遗忘后测试可疑类 Acc ≈ 10%，其余类 Acc 不降 | ΔAcc_clean < 1% |
| 理论界紧致度 | 实测遗忘界 `ε_emp` vs 理论 `ε_c` | `ε_emp ≤ 1.2 · ε_c` |
| 端到端时间 | 完整一轮：检测+掩码+聚合+遗忘 | < 30s / 轮 (GPU) |

---

*文档生成时间：2026‑07‑20*  
*存放路径：`/mnt/d/VS Code 项目/label_modify_fl/异常客户端处理.md`*