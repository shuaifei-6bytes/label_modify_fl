# 良性客户端处理（余弦相似度检测后）设计文档

## 1. 背景
服务器通过 **余弦相似度检测**（见《服务器端恶意检测.md》）判定出：
- 每类污染分数 `p_c ∈ [0,1]`
- 可疑客户端集合 `S_c`

本文档描述：对 **未被标记为可疑的良性客户端** 执行 **带界聚合 + 参数锁定**，确保：
1. 恶意更新不污染全局模型
2. 干净类知识完全保留
3. 全程零额外超参，全部由 `p_c` 驱动

---

## 2. 良性客户端识别
```
benign_clients = all_clients \ (∪_c S_c)
```
- `S_c` 中的客户端：本轮**不参与聚合**，仅执行本地遗忘（见《异常客户端处理.md》）
- `benign_clients`：正常参与聚合

---

## 3. 带界聚合（双重保险）

### 3.1 污染度加权
```
w_k = 1 / (1 + max_c p_c)      # 该客户端关联的最脏类分数
w_k = w_k / Σ_j w_j
```
- `p_c = 0`（全干净） → `w_k = 1`，满权重
- `p_c → 1`（极脏） → `w_k → 0`，权重趋零

### 3.2 Trimmed‑Mean 兜底
对加权更新做 **Trimmed‑Mean**：
```
global = TrimmedMean({w_k · Δ_k}, trim_ratio = 0.2)
```
- 剔除极值 20% → 再次防御残余异常
- 配合加权形成 **双重保险**

---

## 4. 类级参数锁定（掩码下发）

服务器下发每类掩码 `M_c`（构建见《异常客户端处理.md》）：

| 类别状态 | 掩码 `M_c` | 本地行为 |
|----------|------------|----------|
| `p_c = 0`（干净） | `∅` | **全参数正常本地更新** |
| `0 < p_c ≤ 1`（疑似脏） | Top‑k(|ĝ_c|, p_c·ratio_max) | **锁死 `M_c` 内参数**，仅更新 `M_c` 外参数 |

**客户端本地实现（伪代码）：**
```python
def local_train(model, loader, masks, epochs, lr):
    optimizer = SGD([p for n,p in model.named_parameters() 
                     if n not in masks], lr=lr)
    for _ in range(epochs):
        for x,y in loader:
            loss = CE(model(x), y)
            loss.backward()
            # 锁死掩码内参数梯度
            for n in masks:
                model.state_dict()[n].grad.zero_()
            optimizer.step()
```

---

## 5. 参数零额外超参声明

| 参数 | 来源 | 备注 |
|------|------|------|
| 加权权重 `w_k` | `p_c` 自动计算 | 无需人工设定 |
| Trimmed‑Mean `trim_ratio` | 固定 0.2 | 经典鲁棒聚合标准值 |
| 掩码比例 `ratio_c` | `p_c · ratio_max` | `ratio_max` 仅 1 个全局常数 |
| 遗忘轮数/学习率 | `p_c` 线性映射 | 见《异常客户端处理.md》 |

**全流程仅 1 个全局常数 `ratio_max`（如 0.1），其余全部 `p_c` 自动驱动。**

---

## 5. 理论界自动同步（复用）

| 理论界 | 参数 |
|--------|------|
| Rényi 遗忘界 `ε_c` | `ε_c = ε₀ · p_c` |
| Anti‑Relearning 正则 `λ_mem,c` | `λ_mem,c = λ₀ · p_c` |

无需人工干预，检测输出 `p_c` 即驱动全链路。

---

## 6. 算法完整伪代码（良性端视角）

```text
Input: 当前全局模型 θ, 良性客户端更新 {Δ_k}, 污染分数 {p_c}, 掩码 {M_c}
Output: 新全局模型 θ_new

# 1. 计算权重
for k in benign_clients:
    p_max_k = max_{c∈client_k_classes} p_c
    w_k = 1 / (1 + p_max_k)
w_k = w_k / Σ w_k

# 2. 加权聚合
θ_agg = Σ_k w_k · Δ_k

# 3. Trimmed‑Mean
θ_new = TrimmedMean({w_k · Δ_k}, 0.2)

# 4. 下发掩码 {M_c}
# 客户端本地训练时：
#   - p_c=0 类：全参数更新
#   - p_c>0 类：锁死 M_c 内参数，仅更新外部

return θ_new
```

---

## 7. 给评审/开题的一句话总结

> “良性客户端走 **加权 Trimmed‑Mean 双重鲁棒聚合**，同时下发 **类级掩码 `M_c`** 锁死可疑类参数：  
> `p_c=0` 类全参数更新，`p_c>0` 类仅更新非掩码参数。  
> 全程 **零新增超参**，`p_c` 一键驱动聚合权重、掩码宽度、遗忘强度、理论界参数，形成 **检测→聚合→锁定→遗忘** 完整闭环。”

---

*文档生成时间：2026‑07‑20*  
*存放路径：`/mnt/d/VS Code 项目/label_modify_fl/良性客户端处理.md`*